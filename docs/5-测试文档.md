# 测试文档

## 1. 测试策略

### 1.1 测试金字塔

```
        ┌───────────┐
        │    E2E    │    少量冒烟测试
        │   Tests   │
        ├───────────┤
        │ Integration│    API路由测试
        │   Tests    │
        ├───────────┤
        │   Unit     │    核心业务逻辑
        │   Tests    │
        └───────────┘
```

### 1.2 覆盖率目标

| 层级 | 覆盖率目标 |
|------|-----------|
| Services | 90%+ |
| Repositories | 80%+ |
| API Routes | 80%+ |
| Overall | 80%+ |

## 2. 测试框架

### 2.1 后端测试

- **测试框架**: pytest
- **测试客户端**: FastAPI TestClient
- **Mock**: pytest-mock
- **Fixtures**: pytest fixtures

### 2.2 目录结构

```
backend/tests/
├── conftest.py              # 共享 fixtures
├── api/
│   ├── __init__.py
│   ├── test_projects.py
│   ├── test_episodes.py
│   ├── test_characters.py
│   ├── test_scenes.py
│   ├── test_storyboards.py
│   └── test_models.py
├── services/
│   ├── __init__.py
│   ├── test_project_service.py
│   ├── test_extraction_service.py
│   └── test_video_service.py
└── repositories/
    ├── __init__.py
    ├── test_project_repository.py
    └── test_episode_repository.py
```

## 3. 编写规范

### 3.1 测试命名

```python
def test_project_create_success():
    """创建项目成功"""

def test_project_create_with_invalid_name():
    """使用无效名称创建项目"""

def test_project_list_with_pagination():
    """分页获取项目列表"""

def test_project_search_by_keyword():
    """通过关键词搜索项目"""
```

### 3.2 测试结构 (AAA)

```python
def test_example():
    # Arrange - 准备测试数据
    data = {"name": "test"}

    # Act - 执行被测操作
    result = service.create(data)

    # Assert - 验证结果
    assert result.name == "test"
```

### 3.3 Mock 使用

```python
from unittest.mock import Mock, patch

def test_generate_image():
    with patch("app.services.image_service.ImageModelStrategy") as mock:
        mock.return_value.generate.return_value = {"url": "..."}
        result = service.generate_image(...)
        assert result.url == "..."
```

## 4. API 测试

### 4.1 conftest.py

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from app.main import app
from app.core.database import Base, get_db
from app.models import Project

SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"

engine = create_engine(SQLALCHEMY_DATABASE_URL, connect_args={"check_same_thread": False})
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

@pytest.fixture(scope="function")
def db():
    Base.metadata.create_all(bind=engine)
    db = TestingSessionLocal()
    try:
        yield db
    finally:
        db.close()
        Base.metadata.drop_all(bind=engine)

@pytest.fixture(scope="function")
def client(db):
    def override_get_db():
        try:
            yield db
        finally:
            pass
    app.dependency_overrides[get_db] = override_get_db
    yield TestClient(app)
    app.dependency_overrides.clear()
```

### 4.2 API 测试示例

```python
def test_create_project(client):
    response = client.post(
        "/api/v1/projects",
        json={"name": "测试项目", "description": "测试描述"}
    )
    assert response.status_code == 200
    data = response.json()
    assert data["code"] == 200
    assert data["data"]["name"] == "测试项目"

def test_get_project_not_found(client):
    response = client.get("/api/v1/projects/99999")
    assert response.status_code == 404
```

## 5. Service 测试

### 5.1 Service 测试示例

```python
import pytest
from app.services.project_service import ProjectService
from app.schemas.project import ProjectCreate

def test_project_service_create(db):
    service = ProjectService(db)
    schema = ProjectCreate(name="测试", description="测试描述")
    project = service.create(schema)
    assert project.id is not None
    assert project.name == "测试"

def test_project_service_list_with_pagination(db):
    service = ProjectService(db)
    # 创建测试数据
    for i in range(15):
        service.create(ProjectCreate(name=f"项目{i}"))
    # 测试分页
    result = service.list(page=1, page_size=10)
    assert len(result.items) == 10
    assert result.total == 15
```

## 6. 运行测试

### 6.1 运行所有测试

```bash
cd backend
pytest
```

### 6.2 运行带覆盖率的测试

```bash
pytest --cov=app --cov-report=html
```

### 6.3 运行指定测试

```bash
pytest tests/api/test_projects.py -v
```

### 6.4 生成测试报告

```bash
pytest --html=report.html --self-contained-html
```

## 7. CI/CD 集成

### 7.1 GitHub Actions

```yaml
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -e ".[test]"
      - run: pytest --cov=app
```

## 8. 前端测试

### 8.1 测试框架

- **测试框架**: Vitest + React Testing Library
- **E2E**: Playwright

### 8.2 测试示例

```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import { ProjectList } from '@/pages/ProjectList'

describe('ProjectList', () => {
  it('renders project items', () => {
    render(<ProjectList />)
    expect(screen.getByText('项目列表')).toBeDefined()
  })
})
```

## 9. 测试数据管理

### 9.1 Fixtures 文件

测试数据应使用 YAML/JSON 文件管理：

```
backend/tests/fixtures/
├── projects.yaml
├── episodes.yaml
└── characters.yaml
```

### 9.2 Factory 模式

```python
import factory
from app.models import Project

class ProjectFactory(factory.Factory):
    class Meta:
        model = Project

    name = factory.Sequence(lambda n: f"项目{n}")
    description = "测试描述"
    status = 0
```

## 10. 常用 Mock 场景

| 场景 | Mock 方式 |
|------|-----------|
| 数据库 | SQLAlchemy Session Mock |
| Redis | fakeredis |
| RabbitMQ | aio-pika Mock |
| 外部 API | responses/aioresponses |
| 文件上传 | FastAPI UploadFile Mock |
